# -----------------------------------

general:
  branches:
    only:
      - master

# -----------------------------------

machine:
  timezone:
    UTC
  node:
    version: 6.1.0

# -----------------------------------

dependencies:
  override:
    - npm install -g bower
    - npm install -g ember-cli@2.7.0
    - npm cache clean && bower cache clean
    - npm install && bower install


# -----------------------------------

test:
  override:
    - npm test
